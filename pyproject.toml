[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "entangled-oracle"
version = "0.0.0"
description = "Local-first astrology report generator."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "flask",
    "geonamescache",
    "geopy",
    "jinja2",
    "numpy>=2,<2.5",
    "pyswisseph",
    "timezonefinder",
]

[project.optional-dependencies]
dev = [
    "pytest",
]

[tool.setuptools]
py-modules = [
    "config",
    "generate",
    "product_versions",
    "run_app",
    "test_offline_location",
]

[tool.setuptools.packages.find]
include = [
    "app*",
    "eia_engine*",
    "engine*",
    "formulas*",
    "products*",
    "selectors*",
    "visuals*",
]

[tool.pytest.ini_options]
testpaths = [
    "tests",
    "test_offline_location.py",
]
norecursedirs = [
    ".git",
    ".venv",
    ".pytest_cache",
    ".claude",
    ".codex",
    ".agents",
    "archive",
    "audit_output",
    "entangled-astrology-v0.1",
    "entangled-dashboard-v0.1",
    "node_modules",
    "output",
    "outputs",
    "runtime",
    "tmp",
    "__pycache__",
]
